/**
 * Layout, border radius, and accessibility constants.
 */

export const BorderRadius = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
  xxl: 24,
  full: 9999,
} as const;

export const Layout = {
  minTouchTarget: 44,
  maxContentWidth: 600,
  headerHeight: 56,
  tabBarHeight: 64,
} as const;
